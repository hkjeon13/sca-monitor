[project]
name = "sca-monitor"
version = "0.1.0"
description = "Reported supply chain advisory monitoring MVP"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

